import {
  SheetID,
  SheetName,
  API_KEY
} from './constants';

import DeviceStorage from './storage'

import DimensionScreen from './dimension'

import firebaseNotificationConfig from './firebase'

import PlatformDevice from './platform'

export {
  SheetID,
  SheetName,
  API_KEY,
  DeviceStorage,
  DimensionScreen,
  firebaseNotificationConfig,
  PlatformDevice
}

export default {
  SheetID,
  SheetName,
  API_KEY,
  DeviceStorage,
  DimensionScreen,
  firebaseNotificationConfig,
  PlatformDevice
}